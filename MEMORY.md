# MEMORY.md — память проекта

Файл для восстановления контекста в новом чате. **Обновлять после каждого значимого изменения.**

Последнее обновление: 2026-09-04 (вечер)
Статус: **эта папка — бот №2 для сайта Restock.** Локально всё проверено (клиент→группа, группа→клиент, заявка с сайта через функцию Vercel). На сервер ещё не развёрнут — см. «Деплой #2 (Restock)» ниже. 56/56 тестов проходит.

Репозиторий этого бота: https://github.com/TimurFront/restocktgbot. Исходный бот FINFIX: https://github.com/TimurFront/finfix-bot (публичный — иначе `git clone` на сервере не работает без токена; секретов в коде нет, `.env` не коммитится).
Репозиторий сайта: https://github.com/TimurFront/finfix (проект `C:\projects\FINFIXlanding`, Next.js на Vercel) — интегрирован с ботом, см. раздел 10.

---

## Копируете эту папку для другого сайта? Начните здесь

Эта папка — уже один раз полностью развёрнутый и проверенный бот (сайт FINFIX, см. «Деплой #1» ниже). Всё, что там написано — IP, токен, `GROUP_ID`, секреты — принадлежит **той** заявке и не годится для нового сайта. Но хорошая новость: **код менять не нужно вообще**. Вся разница между «ботом для сайта А» и «ботом для сайта Б» — это только значения в `.env`. Один и тот же код, разные конфиги, разные серверы (или разные порты на одном сервере).

### Нужен ли новый аккаунт Oracle?

**Нет.** Oracle Cloud Always Free разрешает **до 2 инстансов `VM.Standard.E2.1.Micro` на один аккаунт** — заведите второй инстанс на том же аккаунте, оба бесплатны навсегда. Либо ещё проще: второй процесс этого же кода на уже существующем сервере, на другом порту (`PORT=3001` и т.п.) со своим `.env` и отдельной systemd-службой — не нужно даже создавать новую VM, если не важна полная изоляция между сайтами.

### Порядок действий (сжато — все грабли ниже уже учтены заранее)

1. **Новый бот в Telegram**: @BotFather → `/newbot` → сразу же **Bot Settings → Group Privacy → Turn off**. Если забыть — бот не увидит ответы оператора в группе, и это не всплывёт явной ошибкой, просто ничего не будет уходить клиенту.
2. **Новая приватная группа** для нового сайта, добавить второго участника (станет супергруппой), включить **Темы**, добавить бота админом и **отдельно** включить ему право **«Manage Topics»** — админство само по себе этого не даёт.
3. **Новая VM на Oracle** (тот же аккаунт): образ **Ubuntu** (по умолчанию Oracle предложит Oracle Linux — переключить вручную через «Change image»), shape — берите сразу **`VM.Standard.E2.1.Micro`** (Specialty and previous generation), не тратьте время на Ampere A1.Flex — в прошлый раз он не выделился («Out of capacity»). После создания проверьте, что назначился Public IP — иногда не назначается сам, тогда Instance access → **ephemeral public IP**.
4. **`git clone https://github.com/TimurFront/finfix-bot.git`** на сервере (или клонируйте свою копию, если создали отдельный репозиторий). Папка назовётся по имени репозитория — если оно не `finfix-bot`, поправьте пути в `deploy/telegram-relay-bot.service` (`WorkingDirectory`, `EnvironmentFile`) под реальное имя, иначе служба упадёт с `Failed to load environment files`.
5. **`.env`**: новый `TELEGRAM_BOT_TOKEN`, `GROUP_ID` узнаётся из лога после первого сообщения в новую группу (`npm start` в форграунде один раз, дождаться строки в логе), свежий `LEADS_SECRET` (генерировать заново — **не переиспользовать** секрет от FINFIX).
6. **systemd**: `sudo cp deploy/telegram-relay-bot.service /etc/systemd/system/` → `daemon-reload` → `enable --now`. Если разворачиваете второй процесс на том же сервере — юниту нужно **другое имя файла** (не `telegram-relay-bot.service`, иначе перезапишет первого бота) и свой `PORT` в `.env`.
7. **Если сайту нужен приём заявок** (`/leads`) — портов и firewall-слоёв тут **три, не два**: облачный Security List, `ufw` на сервере, и **предустановленные iptables-правила самого образа Ubuntu**, которые блокируют всё кроме порта 22 ещё до того, как их увидит ufw. Третий пункт — самый неочевидный, именно на нём в прошлый раз ушло больше всего времени. Диагностика: `sudo iptables -L INPUT -n --line-numbers -v`, ищите `REJECT` раньше строк `ufw-*`. Подробный фикс — раздел «Грабли» ниже и DEPLOY.md шаг C3.1.
8. **Сторона нового сайта**, если тоже нужна интеграция форм: смотрите `C:\projects\FINFIXlanding\lib\leadsApi.ts` как образец. Принцип: backend сайта сам вызывает `POST /leads/<LEADS_SECRET>` бота — **не Telegram напрямую**, и токен бота никогда не попадает в переменные окружения сайта (только `LEADS_SECRET`, у него совсем другой уровень риска при утечке). Если у нового сайта не Next.js/Vercel, а что-то другое — тот же принцип, просто другой язык.

Подробности каждого шага — в DEPLOY.md, он написан универсально и подойдёт для любого нового деплоя без правок.

---

### Деплой #2 (Restock) — текущее состояние этой папки

- Бот: **@restock_info_bot** (id 8893434279). Токен лежит только в локальном `.env` (в чат его не присылать; первый токен был прислан в чат и отозван через BotFather).
- Группа: **«RESTOCK Сообщения бота»**, `GROUP_ID=-1003615461986`, супергруппа с темами, бот админ с правом «Управление темами». `npm run setup` — все проверки зелёные.
- `.env` заполнен целиком: `MODE=polling`, **`PORT=3001`** (второй процесс на сервере FINFIX, порт 3000 занят первым ботом), свежий `LEADS_SECRET` (не тот, что у FINFIX).
- systemd-юнит переименован в `deploy/restock-bot.service` (имя службы `restock-bot`), чтобы не перезаписать `telegram-relay-bot.service` первого бота на том же сервере. Пути внутри — `/home/ubuntu/restocktgbot`.
- Проверено локально: `/start` в личку → топик «ttttt (@yngdth)»; ответ в топике → пришёл в личку; `POST /leads` → тема «📥 Обращения с сайта» (thread 9).
- **Как получали GROUP_ID**: сообщения из группы бот не видел даже как админ, помог только повторный ввод бота в группу — событие `my_chat_member` содержит id чата. Если снова понадобится — `getUpdates` с `allowed_updates: ['my_chat_member']` и удалить/добавить бота.
- **Сайт Restock** (`C:projectsestock`, статический HTML на Vercel, репо TimurFront/restock): добавлена Vercel Function `api/demo.js` (honeypot, минимальное время заполнения, rate limit, валидация → `POST /leads`), форма в `js/main.js` переведена с имитации на реальный fetch. Коммит `ef2bf5c` **локальный, не запушен** — пуш = автодеплой на Vercel. Перед пушем в Vercel нужно задать `LEADS_API_URL=http://<IP сервера>:3001` и `LEADS_SECRET` из `.env` бота.
- **План деплоя**: второй процесс на существующей VM FINFIX (129.225.90.109): `git clone restocktgbot` → скопировать `.env` → `npm ci && npm run build` → `sudo cp deploy/restock-bot.service /etc/systemd/system/` → `enable --now restock-bot` → открыть порт 3001 на трёх уровнях (Security List, ufw, преднастроенный iptables — см. Грабли). Не трогать службу `telegram-relay-bot`.

### Деплой #1 (FINFIX) — параметры доступа, для истории

- **Public IP: `129.225.90.109`**. Приватный SSH-ключ у пользователя: `C:\Users\45tim\Downloads\ssh-key-2026-09-03.key`. Подключение: `ssh -i "<путь>" ubuntu@129.225.90.109`.
- Tenancy `s5timur45`, регион **India South (Hyderabad)**, инстанс `telegram-bot`, shape **`VM.Standard.E2.1.Micro`** (Ampere A1.Flex не срослось — см. грабли), образ **Canonical Ubuntu 24.04**.
- Код на сервере: `~/finfix-bot` (не `telegram-relay-bot` — имя из шаблона systemd-юнита не совпадало с реальным именем репозитория, пришлось поправить пути в `/etc/systemd/system/telegram-relay-bot.service` через `sed`).
- Служба: `systemctl status telegram-relay-bot` — `enabled`, автозапуск и автоперезапуск настроены. Логи: `sudo journalctl -u telegram-relay-bot -f`.
- Bot: `@finfix_info_bot`. Рабочая группа: **«FINFIX Сообщения боту»**, `GROUP_ID=-1003939584815`, темы включены, у бота есть право «Управление темами» (пришлось включить вручную — админство само по себе его не даёт).
- `MODE=polling`, `PORT=3000`. `LEADS_SECRET` — значение хранится только в `.env` бота на сервере и в Vercel, в репозиторий не записывать.
- В Vercel (проект `finfix`, Settings → Environments → Production) прописаны `LEADS_API_URL=http://129.225.90.109:3000` и `LEADS_SECRET` — то же значение. Проверено вживую: реальная форма сайта → реальный `/leads` → тема в группе.

---

## 1. Что это

Бот-ретранслятор для ручной переписки с клиентами.
Клиент пишет боту → сообщение попадает в приватную группу владельца, **один клиент = один топик форума** → владелец отвечает прямо в топике → бот доставляет ответ клиенту.

**Жёсткое требование заказчика: никакого AI, автоответов и генерации.** Отвечает только человек. Единственное автоматическое сообщение во всей системе — необязательный ответ на `/start` через `WELCOME_TEXT`, и он **выключен по умолчанию**. Есть тест, который это охраняет («бот НИЧЕГО не написал клиенту сам»).

---

## 2. История решения (важно для контекста)

Сначала была построена полноценная веб-панель оператора (React + PostgreSQL + SSE + роли + авторизация). Заказчик посмотрел и сказал, что это избыточно, и попросил **вариант попроще — ретрансляцию в группу с топиками**. Панель удалена целиком по его прямому указанию, репозиторий переписан с нуля. Возвращаться к ней не нужно, если он сам об этом не попросит.

Тогда же был проведён разбор ограничений Telegram, и он остаётся в силе:

- Bot API и MTProto — **две разные платформы**. Бот физически не может читать личные чаты вашего аккаунта.
- **MTProto/userbot (Telethon, GramJS) отвергнут**: серая зона ToS, риск бана номера, session string в БД = полный доступ к аккаунту.
- **Telegram Business Bots** (бот подключается к личному аккаунту, требует Premium у владельца) — рабочая альтернатива, если заказчик захочет, чтобы клиенты писали ему в личку, а не боту. Сейчас не используется.

---

## 3. Архитектура

Один Node-процесс. Хранилище — файл SQLite. Веб-интерфейса нет: рабочее место — обычный Telegram.

```
клиент (личный чат с ботом)
      │  update.message, chat.type === 'private'
      ▼
  handleUpdate → claimUpdate (дедуп по update_id)
      │
      ├─ upsertClient
      ├─ ensureTopic → createForumTopic + карточка клиента
      └─ copyMessage(→ группа, message_thread_id)

оператор (топик в группе)
      │  update.message, chat.id === GROUP_ID, message_thread_id
      ▼
  handleUpdate → фильтры (свой бот, служебные, //, команды)
      └─ copyMessage(→ личный чат клиента) + реакция 👌
```

### Ключевые решения

1. **`copyMessage` вместо разбора типов контента.** Один вызов переносит текст, фото, файлы, голосовые, кружки, стикеры. Не нужно писать ветки под каждый тип и скачивать файлы. Это главное упрощение проекта.
2. **Топики форума как модель диалогов.** Telegram сам даёт список чатов, поиск, непрочитанные и уведомления — их не надо реализовывать.
3. **Ноль runtime-зависимостей.** Node 22 даёт `fetch`, `node:sqlite`, `node:http`. `.env` читает свой загрузчик в 20 строк (`src/env.ts`). В `package.json` только devDependencies.
4. **Повторы внутри `call()` в `telegram.ts`**, а не отдельной очередью с воркером. Для такого объёма достаточно: 429 ждёт `retry_after`, 5xx и сетевые — экспоненциальный backoff, до 4 попыток. 400/401/403 не повторяются.
5. **Вебхук всегда отвечает 200**, даже при внутренней ошибке — иначе Telegram зациклит повторы и заблокирует очередь.
6. **Права = членство в группе.** Отдельной авторизации нет; кто в группе, тот и оператор. Осознанный размен на простоту, записан в README как ограничение.
7. **HTTP-сервер (`server.ts`) запускается всегда**, независимо от `MODE` (polling/webhook) — с добавлением приёма заявок с сайта (`/leads`) появился второй, не связанный с Telegram-режимом публичный вход, которому нужен порт вне зависимости от того, как бот получает апдейты. Раньше сервер поднимался только при `MODE=webhook` — это изменилось при добавлении фичи заявок (см. п.11 ниже).

---

## 4. Обнаруженные ограничения Telegram

- **Group Privacy у бота должен быть выключен** (@BotFather → Bot Settings → Group Privacy → Turn off), иначе бот не видит обычные сообщения в группе и ответы оператора никуда не уйдут. Через API это не проверить — только предупреждение в `npm run setup`. После смены настройки бота надо **удалить из группы и добавить заново**.
- Темы (Topics) есть только у **супергрупп**. Обычная группа становится супергруппой после добавления второго участника.
- Боту нужно право **«Управление темами»**, иначе `createForumTopic` вернёт ошибку.
- Сообщения в топике **General** приходят без `message_thread_id` — к клиенту не привязаны, игнорируются.
- Свои же скопированные сообщения бот получает обратно как апдейты. Без фильтра `from.id === botId` получилось бы зацикливание.
- Удалённый вручную топик даёт `message thread not found` → пересоздаём и повторяем один раз.
- Закрытый топик даёт `TOPIC_CLOSED` → переоткрываем и повторяем.
- Лимиты: ~1 сообщение/сек в один чат, ~20/мин в группу. Реализован троттлинг по chat_id в `telegram.ts`.
- Telegram не гарантирует ровно-однократную доставку вебхука → дедуп по `update_id` в таблице `seen_updates`.
- `getUpdates` и webhook взаимоисключающи (409). Poller сам снимает вебхук при старте.
- Номер телефона клиента приходит, **только если он сам поделился контактом**.
- Альбом из нескольких фото приходит отдельными апдейтами — в топике будет несколько сообщений.

---

## 5. Структура файлов

```
src/
  config.ts              настройки из .env
  env.ts                 загрузчик .env без зависимостей
  log.ts                 логгер
  telegram.ts            Bot API: call() с повторами и троттлингом, обёртки методов,
                         классы ошибок (permanent / topicGone / human)
  store.ts               SQLite: clients, seen_updates, state
  relay.ts               ВСЯ логика ретрансляции: handleUpdate, клиент→группа, группа→клиент, команды
  leads.ts               приём заявок с сайта: валидация, топик, форматирование сообщения
  poller.ts              long-polling
  server.ts              единый http-сервер на node:http: /health, /webhook, /leads
  index.ts               запуск, проверка настроек, graceful shutdown
  scripts/setup.ts       диагностика: токен, группа, темы, права, privacy mode
  scripts/set-webhook.ts регистрация вебхука
  tests/mock-telegram.ts заглушка Bot API (умеет 429, блокировку, удаление топика)
  tests/e2e.ts           51 проверка

deploy/telegram-relay-bot.service   systemd-юнит для автозапуска на сервере
DEPLOY.md                            пошаговый деплой на Oracle Cloud Free Tier
```

Основная логика целиком в `relay.ts` — начинать чтение оттуда.

---

## 6. Схема хранилища

```sql
clients (
  user_id PK, thread_id UNIQUE, first_name, last_name, username,
  language_code, phone, first_seen_at, last_seen_at, banned
)
seen_updates (update_id PK, at)   -- дедупликация, чистится раз в 6 часов
state (k PK, v)                   -- offset для polling
```

Файл: `data/relay.db` (путь в `DB_FILE`). **Требует постоянного диска в production** — если потерять, привязка клиентов к топикам исчезнет и всем создадутся новые топики.

---

## 7. Настройки

| Переменная | Зачем | Обязательна |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` | токен от @BotFather | **да** |
| `GROUP_ID` | id приватной группы с темами | **да** |
| `MODE` | `polling` (по умолчанию) или `webhook` | нет |
| `PORT`, `PUBLIC_URL`, `WEBHOOK_SECRET_PATH`, `WEBHOOK_SECRET_TOKEN` | только для webhook | для webhook |
| `DB_FILE` | путь к файлу БД | нет |
| `WELCOME_TEXT` | приветствие на `/start`; **пусто = бот молчит** | нет |
| `CONFIRM_WITH_REACTION` | ставить 👌 после доставки | нет |
| `LOG_LEVEL` | `debug` / `info` / `warn` / `error` | нет |
| `TELEGRAM_API_BASE` | подмена адреса Bot API, **только для тестов** | нет |
| `LEADS_SECRET` | включает приём заявок с сайта (`/leads/<secret>`); пусто = функция выключена | нет |
| `LEADS_TOPIC_NAME` | название темы для заявок, по умолчанию «📥 Обращения с сайта» | нет |
| `LEADS_CORS_ORIGIN` | Access-Control-Allow-Origin, по умолчанию `*` | нет |

`GROUP_ID` узнаётся сам: добавьте бота в группу, напишите там что-нибудь — id появится в логе.

**Важно:** если задан `LEADS_SECRET`, HTTP-порт (`PORT`, по умолчанию 3000) должен быть открыт снаружи **независимо от `MODE`** — даже в `polling` придётся открыть порт на обоих уровнях firewall Oracle (Security List + ufw). Раньше (до фичи заявок) `polling` вообще не требовал открытых портов — это больше не так, если заявки включены. Подробности — DEPLOY.md, шаг C3.1.

---

## 8. Что сделано

- [x] Приём сообщений клиентов, создание топика на клиента, карточка клиента
- [x] Доставка ответов оператора клиенту, подтверждение реакцией
- [x] Любой контент в обе стороны через `copyMessage`
- [x] Команды `/info` `/id` `/close` `/ban` `/unban` `/stats` `/help`
- [x] Внутренние заметки через префикс `//`
- [x] Защита от зацикливания на собственных сообщениях
- [x] Дедупликация апдейтов, пересоздание удалённых топиков, переоткрытие закрытых
- [x] Повторы при 429/5xx, троттлинг под лимиты, понятные оператору тексты ошибок
- [x] Два режима: polling и webhook (с двухслойной защитой вебхука)
- [x] `npm run setup` — диагностика настройки
- [x] Заявки с сайта: `POST /leads/<LEADS_SECRET>` → отдельная тема «📥 Обращения с сайта» в группе. Контракт зеркалит реальную форму FINFIXlanding (`DemoRequest`): `kind: "full"` (name, company, **phone**, comment, plan, module) и `kind: "quick"` (только phone, карточка «Консультация»). Изначально в полной заявке был `telegram`-username, но сайт перешёл на телефон с кодом страны — контракт бота обновлён вслед (см. раздел 10). Сообщение бот собирает сам из полей, обычным текстом — не доверяет чужой разметке
- [x] Git-репозиторий инициализирован и запушен на GitHub (TimurFront/finfix-bot)
- [x] DEPLOY.md — пошаговая инструкция на Oracle Cloud Free Tier + systemd-юнит для автозапуска
- [x] 56 сквозных тестов с заглушкой Bot API (было 41 → +10 на заявки → +4 на контракт full/quick → +1 на переход telegram→phone)
- [x] **Интеграция с сайтом FINFIXlanding доведена до конца** — см. раздел 11. Токен бота больше не хранится в Vercel, сайт вызывает `/leads` вместо прямого Telegram API. Проверено сначала сценарием с заглушкой, потом **живьём на проде**
- [x] **Развёрнут на Oracle Cloud и работает в production** — см. «Продакшен-сервер» выше. Systemd-служба, автозапуск, оба уровня firewall под `/leads` открыты, Vercel настроен, реальная заявка с сайта дошла до Telegram-группы

---

## 9. Что можно добавить (не блокирует работу)

- **Ответ на конкретное сообщение клиента.** Сейчас `copyMessage` не переносит связь reply. Можно хранить пары `message_id` в группе ↔ в чате клиента и подставлять `reply_parameters`.
- **Редактирование сообщений** (`edited_message`) — сейчас апдейт не запрашивается и игнорируется.
- **Альбомы** — собирать `media_group_id` и слать одним `sendMediaGroup`.
- **Быстрые ответы** — заготовленные тексты по команде вроде `/t1` (вставляет человек, это не автоответ).
- **Автозакрытие топиков** после N дней тишины.
- **Переезд на Telegram Business**, если заказчик захочет, чтобы клиенты писали в его личку. Токен и логика ретрансляции переиспользуются, меняется тип апдейта (`business_message`) и добавляется `business_connection_id` при отправке. Нужен Telegram Premium.
- ~~HTTPS для эндпоинта заявок~~ — **закрыто**. Оказалось, что не нужно: у сайта есть свой backend (Next.js API route на Vercel), вызов идёт сервер-сервер, браузерные ограничения (mixed content/CORS) не действуют. См. раздел 11.

---

## 10. Интеграция с сайтом FINFIXlanding

Отдельный проект: `C:\projects\FINFIXlanding`, Next.js 16, деплой на Vercel, репозиторий https://github.com/TimurFront/finfix.

**История вопроса.** Изначально был вопрос: как форма на сайте отправляет заявку — из браузера или через backend? Оказалось — через backend (`app/api/demo/route.ts`, Next.js Route Handler), и раньше он вызывал `api.telegram.org/bot<token>/sendMessage` напрямую, храня **полный токен бота** в переменных окружения Vercel. Это плохо не из-за HTTP/HTTPS (моё исходное предположение про mixed content было ошибочным — пользователь верно поправил: `api.telegram.org` и так HTTPS), а из-за того, что токен = полный доступ к боту, и держать его в двух местах (Oracle-сервер + Vercel) увеличивает поверхность утечки.

**Сделано:**
- `lib/telegram.ts` → `lib/leadsApi.ts`: теперь вызывает `POST {LEADS_API_URL}/leads/{LEADS_SECRET}` бота вместо прямого Telegram API.
- Убран мёртвый код: `buildMessage()` и `escapeHtml()` (были нужны только для HTML-разметки прямого вызова Telegram; бот теперь форматирует сообщение сам, обычным текстом).
- `.env.example` сайта: `TELEGRAM_BOT_TOKEN`+`TELEGRAM_CHAT_ID` → `LEADS_API_URL`+`LEADS_SECRET`.
- `app/api/demo/route.ts` — тронута только строка импорта. Вся его логика (honeypot-поле `website`, минимальное время заполнения формы 2 сек, rate limit 5 запросов/10 мин на IP, валидация в `lib/validation.ts`, очистка ввода в `lib/sanitize.ts`) осталась как была — это качественный, самодостаточный слой защиты, дублировать его на боте не нужно.
- Контракт бота (`src/leads.ts`) подстроен под реальную форму сайта: `kind: "full"|"quick"`, поля `name/company/phone/comment/plan/module` (полная форма) или `phone` (быстрая заявка из карточки «Консультация»). Это зеркалит `DemoRequest`/`LeadKind` из `lib/validation.ts` сайта.
- Закоммичено и запушено: finfix-bot `00917dd`, finfix `c5993b7`.
- **Позже форма демо на сайте перешла с Telegram-username на телефон с выбором кода страны** (сайт: коммит `9a45d62`, бот: коммит `e0d7be0`) — контракт `kind:"full"` обновлён вслед, поле `telegram` убрано из `LeadInput` целиком, вместо него везде `phone` с общей для обоих видов заявки валидацией (`+` и 10–15 цифр). Это было сделано в **отдельной сессии** (не в этой), и после неё **сервер какое-то время работал по старому контракту** — код и Vercel обновились, а Oracle-сервер нет, из-за чего полные заявки реально отклонялись, пока не подтянули обновление вручную (`git pull && npm run build && systemctl restart`). Отсюда практический вывод: **после любого коммита в этот репозиторий не забывать передеплоить сервер** — автообновления нет, `git pull` на сервере никто не делает сам.

**Проверено сквозным сценарием** (не юнит-тестами по отдельности, а живой связкой): реальный собранный `next start` сайта делает реальный `POST /api/demo` → реальный собранный `dist/index.js` бота → заглушка Bot API. Проверены: полная заявка, быстрая заявка, и что honeypot-спам отвечает `200 ok` фронту, но **не долетает до бота** (in-memory rate limit и honeypot внутри route.ts единственная линия защиты перед ботом — сам `/leads` эндпоинт бота собственного rate-limit не имеет, полагается на секрет в пути).

**Статус: полностью готово, всё сделано.**
1. ~~Доразвернуть бота на Oracle~~ — готово.
2. ~~В Vercel прописать `LEADS_API_URL`/`LEADS_SECRET`~~ — готово (Settings → Environments → Production).
3. ~~Сквозная проверка на проде~~ — готово: реальная заявка с реального сайта дошла до темы «📥 Обращения с сайта».

---

## 11. Грабли

- **Group Privacy** — причина №1, по которой «бот не отвечает». Проверяется только глазами.
- Без фильтра `from.id === botId` бот зацикливается на собственных копиях.
- В Git Bash на Windows кириллица в аргументах `curl` ломается кодировкой — тестовые данные слать только из Node-скриптов.
- В тестах заголовки HTTP не принимают не-ASCII: секреты только латиницей.
- `node:sqlite` синхронный и помечен экспериментальным — при старте Node печатает `ExperimentalWarning`. Это нормально.
- Фоновые процессы, запущенные через Bash-тул, умирают вместе с шеллом; для ручных проверок запускать через `nohup` или отдельным окном.
- В PowerShell (Windows) `&&` работает только в новых версиях; в старой Windows PowerShell 5.1 команды нужно выполнять по отдельности.
- После добавления `/leads` HTTP-сервер стал нужен всегда (см. решение №7 в разделе 3) — если в будущем добавится ещё один HTTP-вход, добавлять его тоже в `server.ts`, а не создавать третий отдельный сервер/порт.
- **Oracle Console UI разошёлся с текстом DEPLOY.md** в паре мест (интерфейс меняется со временем): кнопка скачивания ключа называется «Download private key», а не «Save private key»; при первом создании VM образ по умолчанию — Oracle Linux, Ubuntu нужно выбрать вручную через «Change image»; сети (VCN) при первом инстансе ещё нет — нужно выбрать «Create new virtual cloud network», а не «Select existing»; свежесозданный инстанс может не получить Public IP автоматически — назначается вручную через Instance access → «ephemeral public IP» (не «reserved», та ссылка ведёт в документацию, а не в форму назначения) → либо через Networking → VNIC → **IP administration** → Edit у приватного IP. Мастер создания VM — один длинный скроллящийся список шагов на одной странице, не отдельные страницы — это иногда сбивает с толку в скриншотах.
- **VM.Standard.A1.Flex** даёт `Out of capacity for shape ... in availability domain AD-1` — подтверждённый на практике риск. Лечится переключением на `VM.Standard.E2.1.Micro` через Edit → Basic information → Change shape → Specialty and previous generation, без потери остальных уже заполненных шагов мастера.
- **Шаблон `deploy/telegram-relay-bot.service` содержит захардкоженный путь `/home/ubuntu/telegram-relay-bot`.** Если репозиторий клонирован под другим именем (у нас — `finfix-bot`, по имени репозитория на GitHub) — служба падает с `Failed to load environment files: No such file or directory` / `Result: resources`. Чинится `sed`-подстановкой пути в `/etc/systemd/system/telegram-relay-bot.service` после копирования, **до** `daemon-reload`. Стоит поправить сам файл в репозитории, чтобы путь не был захардкожен — либо генерировать юнит скриптом, подставляя реальный `$(pwd)`.
- **Настоящая причина, по которой открытый в ufw порт всё равно не был доступен снаружи**: у образа Ubuntu на Oracle **заранее прописаны iptables-правила** (видимо, часть базового образа) — INPUT-цепочка вида `ACCEPT NEW tcp dpt:22` → `REJECT всё остальное` → и только **после** этого REJECT идут цепочки `ufw-*`. То есть пакеты на любой порт, кроме 22, отклоняются ДО того, как их вообще увидит ufw — сам ufw в такой ситуации совершенно бессилен, сколько бы правил в нём ни добавляй. Диагностика: `sudo iptables -L INPUT -n --line-numbers -v` — если REJECT стоит раньше `ufw-before-input` и других `ufw-*` цепочек, вот она, причина. Фикс: `sudo iptables -I INPUT <номер_перед_REJECT> -m state --state NEW -p tcp --dport <порт> -j ACCEPT`, затем `sudo netfilter-persistent save` (доустановить `iptables-persistent`, если нет). Это стоит внести в DEPLOY.md шаг C3.1 как основную причину проблемы, а не только Security List — Security List на практике не был причиной, всё упиралось именно в этот преднастроенный iptables.
- **Право «Управление темами» боту нужно выдавать отдельно** — то, что бот назначен администратором группы, само по себе это право не включает. Без него `createForumTopic` падает с `Bad Request: not enough rights to create a topic`. Чинится: Группа → Администраторы → бот → включить «Manage Topics» отдельным тумблером.
- **В Telegram-группе есть тема General по умолчанию, и туда же изначально падают тестовые сообщения**, если писать прямо в группу (не боту в личку) — это ожидаемо, `GROUP_ID` в логе появляется именно из таких сообщений в General, когда бот ещё не знает, что это его рабочая группа.
- Периодические `NetworkError: fetch failed` во время long-polling на Oracle — судя по всему, особенность сетевой инфраструктуры Oracle (рвёт подолгу висящие соединения раньше 25-35 сек таймаута). На доставку сообщений не влияет (retry в `poller.ts` сам восстанавливается), но шумит в логе. Не исправлено — можно смягчить, уменьшив `timeout` в `getUpdates`, если будет мешать.
- **«Free Trial» и «Always Free» в Oracle — разные вещи**, баннер про истечение триала пугает раньше времени. Наш `E2.1.Micro` помечен «Always Free-eligible» и продолжит работать бесплатно и после 30 дней триала — предупреждение относится к бонусным кредитам на платные ресурсы, которых мы не касались.
- **Обновления кода на сервер не приезжают сами.** `git push` в репозиторий никак не влияет на уже запущенный процесс на Oracle — нужно руками зайти по SSH и сделать `git pull && npm ci && npm run build && sudo systemctl restart telegram-relay-bot`. Один раз это привело к реальному простою фичи заявок (см. раздел 10) — контракт на сервере отстал от того, что уже отправлял сайт.
